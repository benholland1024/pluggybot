Sequencing decision (from the planning thread) — this issue splits into two parts with opposite timing:

**1. Measurement: now.** pluggybot #2/#3 just changed the perf profile (vectorized grid, noslip policy) and v0 landed `--record`, so measure immediately:
- Free: `MUJOCO_GL=osmesa taskset -c 0-3 uv run python scripts/hub_lifecycle.py --headless` on the dev machine; compare sim time vs wall clock.
- Nearly free, more honest: Hetzner bills hourly — spin up a CCX23 for an afternoon (cents), run the same command on real dedicated cores, destroy it. Answers both "does osmesa keep up at 1×?" and "is CCX23 the right size?" with zero commitment.
- Record the real-time multiple in `docs/pluggyworld.md` and make the osmesa vs ground-truth-perception call.

**2. Standing server: when pluggybot #5 (webserver v1) lands — not before, not much after.** Before v1 there's nothing to run (everything else is locally testable, the box would idle). But don't wait past v1 either: the biggest acknowledged unknown is week-one-of-24/7 sim stability (renderer leaks, stall classes, solver-state restores), so deploy v1 + a dummy sink early and let it soak while the site's live layer is built in parallel.

The box choice is nearly commitment-free (hourly billing, no contract, stateless compose deploy — Postgres stays with the website), so there's no value in delaying the decision "in case a different option makes more sense" — the measurement is what settles that, and it's step 1.
